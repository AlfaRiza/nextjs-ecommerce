import { type LocaleInterface } from '../../interface'

const locale: LocaleInterface = {
  lbl_signin: 'Sign In',
  lbl_email: 'Email',
  lbl_password: 'Password',
  lbl_login: 'Login',
  lbl_dont_has_account: 'Don`t has account?'
}

export default locale
